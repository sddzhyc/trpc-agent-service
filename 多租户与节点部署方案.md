# tRPC-Agent 多租户与节点部署方案

## 1. 文档目的

本文依据根目录的《实战任务书.md》，结合当前仓库 `trpc-agent-python` 的实际代码和文档，设计一套可落地的多租户 Agent 平台方案。

方案目标是：

- 一套 Agent Worker 服务多个相互隔离的租户；
- 支持企业微信和 Telegram 两类 IM 通道，并保留 HTTP、AG-UI、A2A 接入能力；
- 支持 Worker 多节点水平扩展，普通请求不依赖 sticky session；
- 支持租户级 Agent、模型、工具、数据后端、审计策略配置；
- 能够追踪一次请求从 IM 回调到 Runner、模型、工具、Session/Memory 和 IM 回复的完整链路；
- 对重复投递、节点故障、模型超时、工具失败和配置回滚有明确处理策略。

本文是架构和实施方案，不会把 tRPC-Agent 当前没有提供的能力误写成 SDK 内置能力。租户控制面、鉴权、分布式锁、审计库、对象存储 Adapter、配置发布和 Kubernetes 运维由平台业务层负责。

## 2. 设计原则

### 2.1 租户是隔离边界

所有外部请求先解析并验证 `tenant_id`，再加载租户配置。租户身份不能由客户端任意传入后直接信任，也不能只作为日志字段存在；它必须参与配置、Session、Memory、Knowledge、Artifact、权限和审计的命名空间设计。

### 2.2 Worker 尽量无状态

Agent Worker 不保存依赖单进程内存的业务状态。请求进入任意 Worker 后，通过共享的 Redis/SQL 等后端读取 Session 和 Memory，执行完成后写回共享后端。`InMemorySessionService` 和 `InMemoryMemoryService` 只用于本地开发、单节点演示和测试。

### 2.3 先鉴权和治理，再执行 Agent

Gateway 完成租户、用户、通道和幂等校验；Worker 通过 `AgentContext`/`InvocationContext` 和 Filter 再做一次工具、模型、预算和敏感数据控制。任何来自 IM 或 HTTP 的用户输入都不能绕过治理层直接触发工具。

### 2.4 事件是执行事实

tRPC-Agent 的 `Runner.run_async()` 以异步事件流输出 Agent 事件。平台以事件作为回复转换、审计、指标和恢复的事实来源；流式事件只用于实时传输，持久化事件遵循 SessionService 的非 partial 事件语义。

## 3. 总体架构

```text
                 ┌──────────────────────────────────────┐
                 │        外部用户与管理端                │
                 │ 企业微信 / Telegram / Web / A2A / Admin │
                 └───────────────────┬──────────────────┘
                                     │ HTTPS / Webhook / SSE
                   ┌─────────────────▼─────────────────┐
                   │ Agent Gateway / FastAPI            │
                   │ 鉴权、租户解析、限流、幂等、路由、审计 │
                   └──────────┬───────────────┬────────┘
                              │               │
              ┌───────────────▼──────┐  ┌────▼────────────────┐
              │ Channel Adapter       │  │ Admin API            │
              │ WeCom / Telegram      │  │ 租户、应用、版本、策略 │
              │ 消息验签与格式转换      │  │ 配置发布与回滚         │
              └───────────────┬──────┘  └────┬────────────────┘
                              │ 标准 Agent 请求  │ 配置快照
                   ┌──────────▼───────────────▼─────────┐
                   │ Agent Worker 集群                   │
                   │ Runner + LlmAgent + Filter/ToolSet │
                   │ OpenAI/Anthropic/LiteLLM Model     │
                   └──────┬─────────────┬───────────────┘
                          │             │
       ┌──────────────────▼───┐   ┌─────▼──────────────────┐
       │ Shared Data Plane     │   │ Telemetry / Audit Plane │
       │ Redis/Cluster Session │   │ OpenTelemetry Collector │
       │ SQL Session           │   │ Metrics / Trace Backend │
       │ Memory / VectorStore  │   │ Audit Log Store         │
       │ Object Storage        │   └────────────────────────┘
       └──────────────────────┘
```

### 3.1 组件职责

| 组件 | 主要职责 | tRPC-Agent 对应能力 | 平台侧补充 |
|---|---|---|---|
| Agent Gateway | 接收请求、鉴权、租户解析、限流、幂等、路由 | 可用 FastAPI 示例作为 HTTP 基础 | JWT/API Key、租户策略、请求签名、幂等表 |
| Channel Adapter | IM webhook、验签、消息转换、回复发送 | `trpc-claw` 的 MessageBus、WeCom、Telegram 适配参考 | 多租户绑定、重试、平台限制、消息去重 |
| Agent Worker | 执行 Agent 并产生事件 | `Runner`、`LlmAgent`、Event、Tool/Model Filter | 租户配置快照、并发控制、业务审计 |
| Session Adapter | 保存会话事件、状态、摘要 | `RedisSessionService`、`RedisClusterSessionService`、`SqlSessionService` | 租户前缀、锁/版本、迁移、备份 |
| Memory Adapter | 保存跨 Session 长期记忆 | `RedisMemoryService`、`RedisClusterMemoryService`、`SqlMemoryService`、Mem0/Mempalace | 租户 key、索引、跨后端迁移 |
| Knowledge Adapter | RAG 文档检索 | `KnowledgeBase`、`LangchainKnowledge`、`KnowledgeFilterExpr` | 租户元数据过滤、知识库生命周期 |
| Artifact Adapter | 文件、图片、附件、版本 | `ArtifactServiceABC`，当前仓库提供 `InMemoryArtifactService` | S3/OSS/COS 实现、病毒扫描、权限和生命周期 |
| Admin API | 租户与配置控制面 | 无完整内置控制面 | 数据库、版本、发布、回滚、RBAC |
| Telemetry Collector | Trace、Metric、成本和审计采集 | OpenTelemetry tracing/metrics | Collector、采样、脱敏、后端和告警 |

## 4. 租户模型

### 4.1 核心实体

```text
Tenant
 ├── AgentApp
 │    ├── ModelConfig
 │    ├── ToolPolicy
 │    ├── DataBackendConfig
 │    └── AuditPolicy
 ├── ChannelBinding
 ├── TenantUser
 ├── KnowledgeSpace
 └── SecretReference
```

建议的租户配置字段如下：

| 实体 | 关键字段 | 说明 |
|---|---|---|
| tenant | `tenant_id`, `status`, `plan`, `quota`, `config_version` | 租户身份、状态、套餐和当前配置版本 |
| agent_app | `app_id`, `tenant_id`, `agent_name`, `instruction`, `release_state` | 一个租户可拥有多个 Agent 应用 |
| model_config | `provider`, `model_name`, `base_url`, `secret_ref`, `retry_policy`, `token_budget` | API Key 只保存密钥引用，不保存明文 |
| tool_policy | `allow_tools`, `deny_tools`, `mcp_endpoints`, `approval_rules` | 工具白名单、MCP 和危险操作策略 |
| channel_binding | `channel`, `external_account_id`, `webhook_path`, `secret_ref`, `verify_policy` | 一个 IM 账号绑定一个租户和应用 |
| data_backend_config | `session_backend`, `memory_backend`, `knowledge_backend`, `artifact_backend` | 允许租户选择 Redis、SQL、向量库等 |
| audit_policy | `retention_days`, `redaction_rules`, `trace_sample_rate` | 审计保留和脱敏规则 |

### 4.2 tRPC-Agent 标识映射

`Runner` 的实际入口是：

```python
async for event in runner.run_async(
    user_id=user_id,
    session_id=session_id,
    new_message=user_content,
):
    ...
```

平台统一采用以下命名规则，防止不同租户在 SDK 的 `app_name/user_id/session_id` 空间中碰撞：

```text
app_name  = tenant:{tenant_id}:app:{app_id}
user_id   = tenant:{tenant_id}:user:{opaque_user_id}
session_id = hash(tenant_id, app_id, channel, chat_scope, external_user_id)
```

其中：

- 单聊的 `chat_scope` 使用外部用户 ID；
- 群聊的 `chat_scope` 使用外部群 ID，默认一个群对应一个 Session；
- 若需要群内按用户隔离，则将用户 ID 纳入哈希；
- `external_user_id` 先映射成租户内不可逆或不可枚举的用户标识；
- 不接受跨租户复用相同的 `app_name` 或裸 `user_id`。

租户上下文还应放入 `AgentContext` 元数据，供 Filter、Tool 和业务 Telemetry 使用。敏感 Token、API Key 和数据库密码不进入元数据。

## 5. Worker 执行链路

### 5.1 请求流程

```text
1. IM webhook / HTTP 请求到 Gateway
2. 校验签名、Token、时间戳和租户绑定
3. 以外部 message_id 做幂等检查
4. 解析 tenant_id、app_id、user_id、chat_scope
5. 生成规范化 app_name、user_id、session_id
6. 读取已发布的租户配置快照
7. 选择或创建 SessionService、MemoryService、Knowledge 和 ToolSet
8. Worker 调用 Runner.run_async()
9. 消费 Event：审计、指标、协议转换、回复发送
10. Runner 完成 post-turn summary/memory 处理
11. 确认消息投递并更新幂等状态
```

### 5.2 Runner 与服务绑定

`Runner` 支持注入：

- `session_service`：Session 事件、状态和摘要；
- `memory_service`：跨会话记忆；
- `artifact_service`：文件和附件；
- `agent`：`LlmAgent` 或其他 Agent 实现；
- `app_name`：应用命名空间。

生产环境建议在 Worker 进程级缓存共享后端连接和稳定的 Agent/ToolSet；不要为每一条消息创建新的 Redis/SQL 连接池。若按租户动态创建 Runner，应将 `close_session_service_on_close` 和 `close_memory_service_on_close` 设为合适的生命周期策略，避免关闭仍被其他请求使用的共享服务。

### 5.3 模型配置

`LlmAgent` 支持模型实例、模型名称以及模型创建回调。平台推荐使用模型创建回调，通过 `RunConfig.custom_data` 传递已解析的租户配置：

```python
async def create_model(custom_data: dict):
    cfg = custom_data["model_config"]
    return OpenAIModel(
        model_name=cfg.model_name,
        api_key=secret_manager.get(cfg.secret_ref),
        base_url=cfg.base_url,
        model_retry_config=cfg.retry_policy,
    )
```

模型密钥只在调用内存中使用，不写日志、Trace、Session 或 Event。

## 6. 数据与后端设计

### 6.1 统一数据访问抽象

平台定义租户感知的服务工厂：

```python
class TenantRuntime:
    tenant_id: str
    app_name: str
    agent: BaseAgent
    session_service: BaseSessionService
    memory_service: BaseMemoryService | None
    artifact_service: BaseArtifactService | None
    knowledge: KnowledgeBase | None
    tool_policy: ToolPolicy
```

`TenantRuntimeFactory` 根据配置版本构造运行时资源，并将所有后端访问限制到租户命名空间。tRPC-Agent 的 Session/Memory 抽象可以复用，但租户路由和后端工厂是平台层代码。

### 6.2 数据对象存储规划

| 对象 | 首选后端 | 读写特征 | 隔离方式 |
|---|---|---|---|
| Session、Event、State | Redis/Redis Cluster（低延迟）或 SQL（审计型持久化） | 同一 Session 读写频繁 | key/主键包含 `app_name + user_id + session_id` |
| Summary | 与 Session 同库，或单独 Summary 表 | 低频写、读取用于上下文压缩 | 关联 Session 主键和租户 |
| Memory | Redis、SQL、Mem0、Mempalace | 跨会话检索 | `save_key = app_name/user_id`，外部服务也必须带 tenant filter |
| Knowledge | 租户独立 Collection/Index 或共享索引加 metadata filter | 读多写少 | 强制 `tenant_id` 元数据过滤 |
| Artifact | 对象存储；开发阶段可 InMemory | 大文件、版本化 | bucket/prefix + tenant/app/user/session |
| Channel Binding | SQL | 配置和凭据引用 | `tenant_id + channel + external_account_id` 唯一 |
| Audit Log | Append-only SQL/日志仓库 | 只追加，异步写入 | 每条记录必须有 tenant_id |
| Idempotency | Redis SETNX + SQL 归档 | webhook 快速去重 | `tenant_id + channel + message_id` 唯一 |

### 6.3 tRPC-Agent 的实际后端能力

当前仓库明确提供：

- `InMemorySessionService`、`RedisSessionService`、`RedisClusterSessionService`、`SqlSessionService`；
- `InMemoryMemoryService`、`RedisMemoryService`、`RedisClusterMemoryService`、`SqlMemoryService`、Mem0/Mempalace 扩展；
- Session 的 `app:`、`user:`、`temp:` 状态作用域、事件过滤和 TTL；
- Session Summary 管理器和基于阈值的总结；
- `KnowledgeBase` 抽象及基于 LangChain 的 `LangchainKnowledge`；
- `ArtifactServiceABC` 和 `InMemoryArtifactService`。

当前仓库没有直接提供生产级对象存储 ArtifactService、租户控制面、审计表、迁移工具或跨服务事务，需要平台自行实现。

### 6.4 一致性和并发策略

#### Session 并发写入

同一 `session_id` 的消息默认按 Gateway 的 `session_key` 串行化：

1. Redis 分布式锁或租户级消息队列获取写入权；
2. 读取最新 Session；
3. 调用 Runner；
4. 以事件序号/版本号提交 Event、State；
5. 成功后释放锁并触发异步 Summary/Memory。

锁必须有 TTL、续租和故障释放机制。长时间持有锁会降低并发，因此建议以“同一 Session 串行、不同 Session 并行”为粒度。

需要明确：tRPC-Agent 的 Redis/SQL SessionService 提供共享持久化和基础 stale-session 处理，但不等价于完整的跨节点事务锁。`SqlSessionService.append_event()` 源码也明确提示，完整冲突解决需要版本化写入或锁；平台必须补充这一层。

#### 事件、状态、摘要顺序

采用以下顺序：

```text
message accepted
  → user Event persisted
  → model/tool Event persisted
  → final Event persisted
  → session state committed
  → summary generated and committed
  → memory outbox published
```

Summary 和 Memory 允许异步最终一致，但不能覆盖或回写比当前 Session 版本更旧的结果。建议为 Event 增加平台侧 `sequence_no`，为 Summary/Memory 记录 `source_session_version`。

#### Memory 可见性

共享 Redis/SQL 后端可以让其他 Worker 读取已提交 Memory。为避免 Runner 返回后 Memory 尚未写入造成短暂不可见，生产上采用 outbox + 重试，并在需要强一致的请求上等待该 invocation 的 Memory 写入确认。

#### 后端迁移

采用双写/回填/校验/切换流程：

1. 冻结租户配置版本和迁移范围；
2. 从 Redis/本地向量库按租户导出 Session、Event、Memory、Knowledge 文档；
3. 转换为目标 SQL/远端向量库格式，保留原 ID、时间戳、版本和 metadata；
4. 回填期间双写新旧后端；
5. 对数量、哈希、抽样检索结果做校验；
6. 切换租户 `data_backend_config`；
7. 观察期后停止旧后端写入，保留可回滚窗口。

迁移程序必须支持断点续跑、租户级回滚和幂等 upsert。

### 6.5 最小数据模型

```sql
CREATE TABLE tenants (
  tenant_id VARCHAR(64) PRIMARY KEY,
  status VARCHAR(16) NOT NULL,
  config_version BIGINT NOT NULL,
  plan VARCHAR(32),
  created_at TIMESTAMP NOT NULL,
  updated_at TIMESTAMP NOT NULL
);

CREATE TABLE agent_apps (
  tenant_id VARCHAR(64) NOT NULL,
  app_id VARCHAR(64) NOT NULL,
  agent_name VARCHAR(128) NOT NULL,
  instruction TEXT,
  release_version VARCHAR(64) NOT NULL,
  status VARCHAR(16) NOT NULL,
  PRIMARY KEY (tenant_id, app_id)
);

CREATE TABLE sessions (
  tenant_id VARCHAR(64) NOT NULL,
  app_id VARCHAR(64) NOT NULL,
  session_id VARCHAR(128) NOT NULL,
  user_id VARCHAR(128) NOT NULL,
  channel VARCHAR(32),
  chat_scope VARCHAR(128),
  version BIGINT NOT NULL DEFAULT 0,
  state JSON,
  updated_at TIMESTAMP NOT NULL,
  PRIMARY KEY (tenant_id, app_id, session_id)
);

CREATE TABLE message_events (
  tenant_id VARCHAR(64) NOT NULL,
  app_id VARCHAR(64) NOT NULL,
  session_id VARCHAR(128) NOT NULL,
  event_id VARCHAR(128) NOT NULL,
  sequence_no BIGINT NOT NULL,
  invocation_id VARCHAR(128),
  author VARCHAR(128),
  payload JSON NOT NULL,
  partial BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMP NOT NULL,
  PRIMARY KEY (tenant_id, app_id, session_id, event_id),
  UNIQUE (tenant_id, app_id, session_id, sequence_no)
);

CREATE TABLE memories (
  tenant_id VARCHAR(64) NOT NULL,
  memory_id VARCHAR(128) NOT NULL,
  user_id VARCHAR(128),
  session_id VARCHAR(128),
  content JSON NOT NULL,
  source_version BIGINT,
  expires_at TIMESTAMP,
  PRIMARY KEY (tenant_id, memory_id)
);

CREATE TABLE summaries (
  tenant_id VARCHAR(64) NOT NULL,
  app_id VARCHAR(64) NOT NULL,
  session_id VARCHAR(128) NOT NULL,
  source_version BIGINT NOT NULL,
  summary TEXT NOT NULL,
  created_at TIMESTAMP NOT NULL,
  PRIMARY KEY (tenant_id, app_id, session_id, source_version)
);

CREATE TABLE channel_bindings (
  tenant_id VARCHAR(64) NOT NULL,
  channel VARCHAR(32) NOT NULL,
  external_account_id VARCHAR(128) NOT NULL,
  app_id VARCHAR(64) NOT NULL,
  secret_ref VARCHAR(256) NOT NULL,
  webhook_path VARCHAR(256) NOT NULL,
  PRIMARY KEY (tenant_id, channel, external_account_id)
);

CREATE TABLE audit_logs (
  audit_id BIGINT PRIMARY KEY,
  tenant_id VARCHAR(64) NOT NULL,
  channel VARCHAR(32),
  user_id VARCHAR(128),
  session_id VARCHAR(128),
  agent_name VARCHAR(128),
  tool_name VARCHAR(128),
  decision VARCHAR(32),
  latency_ms BIGINT,
  error_type VARCHAR(128),
  cost DECIMAL(18,8),
  trace_id VARCHAR(128),
  payload JSON,
  created_at TIMESTAMP NOT NULL
);
```

## 7. IM Channel Adapter

### 7.1 支持范围

第一阶段支持：

- 企业微信（WeCom）
- 飞书

仓库 `trpc_agent_sdk/server/openclaw` 已提供基于 nanobot 的通道运行时、MessageBus、Telegram/WeCom 接入和流式进度处理，可作为协议和事件转换参考；它不是完整的多租户控制面，租户绑定和审计仍要在平台 Adapter 中增加。

### 7.2 入站消息转换

```python
Content(parts=[Part.from_text(text=normalized_text)])
```

Adapter 负责：

1. 校验 webhook 签名、Token、时间戳和来源账号；
2. 根据 `channel_binding` 找到 `tenant_id/app_id`；
3. 将平台用户 ID 映射为租户内 `user_id`；
4. 将文本、图片、文件、引用和群聊信息转换为 `Content/Part`；
5. 生成稳定的 `session_id`；
6. 以外部 `message_id` 做幂等；
7. 将标准请求提交 Gateway/Worker。

### 7.3 出站 Event 转换

Worker 输出的 Event 可能包含：

- 文本增量；
- `function_call`；
- `function_response`；
- 长任务/人工审批事件；
- 错误和取消事件。

Adapter 根据通道能力转换为：

- Telegram 文本消息、分片和文件；
- 企业微信文本、Markdown、卡片或流式更新；
- 失败时进入重试队列，并通过最终状态避免重复回复。

IM 平台的消息长度、频率、异步回复窗口和撤回语义不由 Runner 解决，必须由 Adapter 的发送队列和限速器处理。

### 7.4 IM 幂等与身份隔离

幂等键：

```text
idempotency_key = tenant_id + channel + external_account_id + message_id
```

处理状态建议为 `RECEIVED → RUNNING → REPLIED/FAILED`。重复 webhook：

- 已 `REPLIED`：直接返回成功，不再次执行；
- `RUNNING`：返回处理中，不创建第二个 Runner invocation；
- `FAILED`：根据重试次数和错误类型决定重新执行或进入死信队列。

跨群、跨租户不能复用 Session。用户可以在同一租户内跨群共享长期 Memory，也可以按策略完全隔离；跨租户必须使用不同的 `tenant_id` 命名空间和授权结果。

## 8. 工具、权限与安全治理

### 8.1 ToolSet 动态白名单

`BaseToolSet.get_tools(invocation_context)` 支持根据当前调用上下文动态返回工具。平台实现 `TenantToolSet`：

```python
class TenantToolSet(BaseToolSet):
    async def get_tools(self, invocation_context):
        policy = runtime_policy(invocation_context)
        return [tool for tool in self._all_tools if policy.allows(tool.name)]
```

安全检查必须在工具真正执行前再次进行，不能只依赖 LLM 是否看到了工具描述。

### 8.2 Filter 与回调

仓库提供 Tool、Model、Agent 三类 Filter，并支持 `before_agent_callback`、`after_agent_callback`、`before_model_callback`、`after_model_callback`、`before_tool_callback`、`after_tool_callback`。

建议策略：

| 阶段 | 规则 |
|---|---|
| Agent before | 检查租户状态、用户身份和剩余预算 |
| Model before | 选择模型版本、限制上下文和 Token 预算 |
| Tool before | 工具白名单、参数校验、敏感字段脱敏、危险操作审批 |
| Tool after | 记录结果摘要、耗时、错误和计费信息 |
| Model after | 统计 Token、首字延迟和模型错误 |
| Agent after | 记录最终决策、Session 版本和审计结果 |

### 8.3 Human-in-the-loop

删除、发消息、转账、写生产数据库等危险工具使用 `LongRunningFunctionTool` 或 Plan Mode 形成待审批事件。审批状态必须写入共享 Session/任务表，不能只留在 Worker 内存中。

### 8.4 密钥和脱敏

- Secret Manager/Vault 只保存 IM Secret、模型 API Key、数据库密码；数据库只保存 `secret_ref`；
- 日志和 Trace 禁止记录完整 Authorization、API Key、Cookie、数据库 URL 密码；
- 用户消息、工具参数和模型输出按租户脱敏规则处理；
- OTel 属性只写 `tenant_id`、应用、会话、模型、工具、耗时和错误类型等必要字段；
- 审计原文采用加密存储和最小权限访问；
- Trace 采样和审计保留时间由租户策略控制。

## 9. 可观测性与审计

### 9.1 Trace 结构

```text
im.callback
  └── gateway.request
       └── runner.invocation
            ├── agent.run
            │    ├── model.call_llm
            │    └── tool.execute
            ├── session.read/write
            ├── memory.read/write
            └── im.reply
```

tRPC-Agent 已提供 OpenTelemetry trace/metric 能力，包括 Runner、Agent、LLM 和 Tool 相关的 trace/metric 函数。Session/Memory 读写、IM callback/reply 的 Span 需要在平台 Adapter 和 Storage Adapter 中补充，并通过当前 OTel context 继承同一 `trace_id`。

### 9.2 指标

至少记录以下指标，并按 `tenant_id`、`app_id`、`channel`、`model` 维度聚合：

- webhook/request 总量、成功率、限流量；
- Agent invocation 数、并发数、排队时长；
- LLM 调用次数、耗时、首 Token 延迟、输入/输出 Token；
- Tool 调用次数、耗时、失败率和拒绝次数；
- Session/Memory 读写延迟、错误率和连接池使用率；
- IM 发送成功率、重试次数、死信数；
- 每租户 Token 消耗、模型成本和预算拒绝数；
- Event、Summary、Memory outbox 积压量。

### 9.3 审计字段

审计记录至少包含：

```text
tenant_id, channel, user_id, session_id, agent_name,
tool_name, decision, latency, error_type, cost, trace_id,
config_version, worker_id, message_id, created_at
```

其中 `decision` 取 `allow/deny/approval_required/success/failure` 等有限枚举，避免把完整敏感参数写入审计日志。

## 10. 故障恢复与运维

### 10.1 故障策略

| 故障 | 处理策略 |
|---|---|
| Worker 节点故障 | Gateway 重试到其他健康 Worker；未提交的 invocation 由幂等/任务状态避免重复执行 |
| IM 重复投递 | `message_id` 幂等表或 Redis SETNX，已处理消息直接确认 |
| Redis/SQL 短暂不可用 | 有界指数退避；无法确认 Session 时不继续执行 Agent，进入重试队列 |
| 模型限流/超时 | 使用 `ModelRetryConfig` 处理可重试错误；超过预算或重试上限返回降级消息 |
| 工具失败 | Tool Filter 记录错误；可重试工具有限重试，不可重试工具返回结构化失败事件 |
| IM 发送失败 | Outbox + 重试 + 死信；回复必须带幂等发送键 |
| Summary/Memory 失败 | 不阻塞主回复；记录 outbox，后台补偿，保留 Session 原始 Event |
| 用户取消 | `Runner.cancel_run_async()` 为协作式取消；多节点必须增加路由或分布式取消机制 |

### 10.2 灰度与回滚

租户配置采用不可变版本：

```text
DRAFT → PUBLISHED → CANARY → ACTIVE → ROLLED_BACK
```

发布时将 `config_version` 固化到每次 invocation。先选择少量租户或流量灰度；异常时只把受影响租户指向上一版本，不修改正在执行的 invocation。

### 10.3 容量评估方法

上线前用压测数据测量以下变量：

```text
并发 Worker 数 ≈ 峰值请求速率 × 平均 Agent 执行时长
LLM QPS       ≈ 请求 QPS × 每请求平均模型调用次数
Tool QPS      ≈ 请求 QPS × 每请求平均工具调用次数
Session QPS   ≈ 请求 QPS × 每请求 Session 读写次数
Memory QPS    ≈ 请求 QPS × Memory 检索/写入次数
```

重点观察：单 Worker 并发 Session 数、模型平均/尾延迟、Token 消耗、Redis/SQL P95 延迟、IM webhook 峰值和 outbox 积压。初始容量建议通过压测确定，不直接使用未经验证的固定数字。

## 11. 部署方案

### 11.1 最小可运行方案（Docker Compose）

```text
gateway/worker（1 个容器，可合并）
redis（Session + Idempotency + Lock）
postgres（租户配置 + Audit + 可选 SQL Session）
otel-collector（可选）
```

配置：

- 使用 `RedisSessionService` 和 `RedisMemoryService`；
- 本地开发可使用 `InMemoryArtifactService`；
- FastAPI 提供 `/health`、聊天和 webhook 入口；
- WeCom/Telegram 使用测试账号；
- 所有配置通过环境变量或 Secret 注入。

### 11.2 生产推荐方案（Kubernetes）

```text
Ingress / API Gateway
  ├── agent-gateway Deployment + HPA
  ├── channel-adapter Deployment + HPA
  ├── agent-worker Deployment + HPA
  ├── admin-api Deployment
  └── otel-collector DaemonSet/Deployment

Managed Redis Cluster
Managed PostgreSQL
Managed Vector DB
Object Storage
Secret Manager / KMS
```

生产要求：

- Worker 使用共享 Redis Cluster/SQL，不使用进程内 Session 作为主存储；
- Readiness 检查模型网关、Redis、SQL 和配置版本是否可用；
- HPA 结合 CPU、活跃 invocation、队列深度和模型延迟；
- Pod 使用优雅退出，停止接收新请求并等待流式任务/Outbox 排空；
- Redis、SQL、向量库和对象存储启用备份、监控和恢复演练；
- A2A Cancel 若采用协议取消，必须配合能把取消请求送到执行节点的路由策略；
- 所有部署和配置变更支持审计。

## 12. 验收标准与预期效果

### 12.1 功能验收

- 两个租户可使用不同模型、工具白名单和 Session/Memory 后端；
- 企业微信和 Telegram 消息都能转换为 tRPC `Content` 并获得回复；
- 单聊、群聊生成稳定且互不串租户的 `session_id`；
- 两个 Worker 轮流处理同一 Session，历史上下文和 Memory 正常；
- 重复 IM webhook 不重复调用模型和工具；
- 危险工具能被拒绝或进入人工审批；
- 租户配置发布、灰度和回滚可追溯；
- 取消、模型超时、工具失败和 IM 重试有可观测结果。

### 12.2 非功能验收

- 租户数据、向量检索、Artifact 和日志均无法跨租户读取；
- Trace 能关联 callback、Runner、LLM、Tool、存储和回复；
- 任何日志、Trace、错误响应均不泄露密钥；
- Session 并发写入不会造成 Event 丢失、乱序或状态回退；
- 节点滚动发布期间，已建立的流式请求有明确的完成或重试结果；
- 完成一次 Redis→SQL 和本地向量库→远端向量库的迁移演练。

## 13. 时间规划

### 13.1 需求分类

#### 功能需求

| 编号 | 任务书范围 | 本方案的功能结果 |
|---|---|---|
| F1 | 多租户与节点部署 | 租户模型、Agent Gateway/Worker/Adapter 拓扑、租户和 Session 路由、无 sticky session 的共享后端运行方式 |
| F2 | 数据同步与多后端支持 | Session、Memory、Summary、Artifact、Knowledge、Audit Log 的访问抽象、后端选择、同步、迁移和一致性处理 |
| F3 | IM 软件接入 | 企业微信和 Telegram Channel Adapter、消息/Event 转换、账号绑定、群聊/单聊 Session、重试和幂等 |

#### 非功能需求

| 编号 | 任务书范围 | 处理方式 |
|---|---|---|
| N1 | 治理、监控和安全 | 不单独占用一周：IM 相关的身份校验、Webhook 验签、用户权限、消息脱敏和投递指标放入 F3；租户隔离、工具治理、Trace、审计、密钥、成本和告警放入 F1/F2 及运维收尾 |
| N2 | 故障恢复与运维 | 作为跨周质量门禁，重点在第 3 周完成故障演练、灰度/回滚、容量评估、部署和上线验收 |

### 13.2 三周安排

| 周期 | 主功能需求| 同周落实的非功能需求 | 交付物与验收点 |
|---|---|---|---|
| 第 1 周：F1 多租户与节点部署 | 建立 `Tenant`、`AgentApp`、配置版本和身份映射；实现 Gateway → Worker 路由；跑通 `LlmAgent + Runner`；使用 `app_name/user_id/session_id` 建立租户命名空间；接入共享 Session/Memory 的最小运行链路 | 租户配置隔离、Session/Memory/Knowledge 的租户边界、ToolSet/Filter 权限框架、密钥引用模型、基础 Trace 上下文和审计字段定义 | 两 Worker 可处理同一租户 Session；不同租户数据不可互读；完成租户/应用/配置/通道/审计模型和配置快照接口 |
| 第 2 周：F2 数据同步与多后端支持 | 接入 `RedisSessionService`/`RedisClusterSessionService`/`SqlSessionService` 和对应 Memory；实现 Summary、Artifact、Knowledge 的统一服务工厂；实现 Session 串行锁、Event 顺序、Memory outbox 和 Redis→SQL/向量库迁移最小流程 | Session 并发一致性、读写延迟、后端故障重试、存储操作指标、跨节点 Memory 可见性、数据加密和后端访问审计 | 两 Worker 共享 Session/Memory 测试；重复写入不丢 Event/不回退 State；完成最小迁移演练、Session/Memory 延迟指标和故障重试测试 |
| 第 3 周：F3 IM 软件接入 | 实现企业微信和 Telegram Adapter；完成 webhook 入站到 `Content/Part`、Event 到文本/卡片/流式回复的转换；实现账号绑定、单聊/群聊 Session 规则、消息去重、发送限速和失败重试 | IM 侧完成验签、用户权限、敏感消息脱敏、IM 投递成功率和错误率指标；运维侧完成 OTel/审计落库、故障恢复、Outbox、灰度回滚、容量压测、Docker Compose/Kubernetes 部署 | 两类 IM 端到端 Demo；重复 webhook 不重复执行；IM 发送失败可重试/进死信；完成 Trace 串链、故障演练、部署清单和上线验收 |


第一阶段优先实现 F1/F2/F3 的最小可运行闭环：Artifact 可先使用 `InMemoryArtifactService` 或简单对象存储适配，Admin API 先提供配置 CRUD 和版本切换，迁移先支持单租户、可断点和幂等回填。完整 Admin UI、复杂跨区域部署、精细计费和多后端双写优化列入第二阶段，但不影响三周 MVP 的功能验收。